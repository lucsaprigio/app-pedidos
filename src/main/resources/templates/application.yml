spring:
  application: 
    name: app-pedidos
  
datasource:
  url: jdbc:postgresql://localhost:5432/orders_db
  username: postgres
  password: senha123
  
jpa:
  hibernate:
    ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080